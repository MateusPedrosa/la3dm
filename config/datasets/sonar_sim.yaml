# sonar_sim dataset config
# Set scan_num to the value printed by pcd_recorder on shutdown.

# Number of scans recorded (update after each recording run)
scan_num: 0

# Imaging sonar max range (m)
max_range: 20.0

# Map height bounds (m, in map frame). Adjust to your environment.
min_z: -15.0
max_z: 0.0

original_size: false
predict: false
